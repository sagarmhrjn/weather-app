import { FormatToLocalTimePipe } from './format-to-local-time.pipe';

describe('FormatToLocalTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatToLocalTimePipe();
    expect(pipe).toBeTruthy();
  });
});
